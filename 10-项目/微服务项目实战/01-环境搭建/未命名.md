  docker login --username=01xqdocker@1079857282388264 crpi-5yw4kk2bxbk3nj6k.cn-hangzhou.personal.cr.aliyuncs.com

  密码：01xqdocker
  登录阿里云镜像仓库

MYSQL
请在本地执行以下命令：

  # 1. 创建工作目录
  mkdir -p D:/DockerServices/mysql-ms/msql-master/{log,conf,data,mysql-files}
  mkdir -p D:/DockerServices/mysql-ms/msql-slave/{log,conf,data,mysql-files}

  # 2. 创建 docker-compose.yaml 文件（内容如下）：

  version: "3.8"
  services:
    mysql-master:
      container_name: mysql-master
      image: crpi-5yw4kk2bxbk3nj6k.cn-hangzhou.personal.cr.aliyuncs.com/01xq/mysql:8.0.20
      restart: always
      ports:
        - 3340:3306
      privileged: true
      volumes:
        - $PWD/msql-master/log:/var/log/mysql
        - $PWD/msql-master/conf/my.cnf:/etc/mysql/my.cnf
        - $PWD/msql-master/data:/var/lib/mysql
        - $PWD/msql-master/mysql-files:/var/lib/mysql-files
      environment:
        MYSQL_ROOT_PASSWORD: "123456"
      command: [
          '--character-set-server=utf8mb4',
          '--collation-server=utf8mb4_general_ci',
          '--max_connections=3000'
      ]
      networks:
        - mysql-ms
    mysql-slave:
      container_name: mysql-slave
      image: crpi-5yw4kk2bxbk3nj6k.cn-hangzhou.personal.cr.aliyuncs.com/01xq/mysql:8.0.20
      restart: always
      ports:
        - 3341:3306
      privileged: true
      volumes:
        - $PWD/msql-slave/log:/var/log/mysql
        - $PWD/msql-slave/conf/my.cnf:/etc/mysql/my.cnf
        - $PWD/msql-slave/data:/var/lib/mysql
        - $PWD/msql-slave/mysql-files:/var/lib/mysql-files
      environment:
        MYSQL_ROOT_PASSWORD: "123456"
      command: [
          '--character-set-server=utf8mb4',
          '--collation-server=utf8mb4_general_ci',
          '--max_connections=3000'
      ]
      networks:
        - mysql-ms
  networks:
    mysql-ms:
      driver: bridge
 3. 复制配置文件
  # 主库配置 my.cnf：

  [mysqld]
  secure-file-priv=NULL
  server-id=1
  log-bin=zos-mysql-bin
  binlog-ignore-db=mysql
  binlog_cache_size=1M
  binlog_format=mixed
  binlog_expire_logs_seconds=604800
  slave_skip_errors=1062

  # 从库配置 my.cnf：

  [mysqld]
  secure-file-priv=NULL
  server-id=2
  log-bin=zos-mysql-bin
  replicate-ignore-db=mysql
  binlog_cache_size=1M
  binlog_format=mixed
  binlog_expire_logs_seconds=604800
  slave_skip_errors=1062
  read_only=1
  