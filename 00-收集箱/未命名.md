  graph TB
      subgraph 功能总览
          A[学员上课状态管理] --> B[获取列表<br/>条件+分页]
          A --> C[设置状态<br/>支持多选-点名]
          A --> D[停课/复课]
          A --> E[还原<br/>消课返还]
      end

  让我分别为每个功能绘制详细流程图：

  1. 获取学员上课状态列表（条件+分页）

  graph TB
      subgraph 获取列表流程
          Start([开始]) --> A[GET /common/lessonStudent/evaluateLog]
          A --> B[接收分页参数]
          B --> C{page参数<br/>是否存在?}
          C -->|是| D[调用getList<br/>分页查询]
          C -->|否| E[调用getAll<br/>查询全部]
          D --> F[MyBatis Plus<br/>分页插件处理]
          E --> G[无分页<br/>直接查询]
          F --> H[markMobiles<br/>处理手机号脱敏]
          G --> H
          H --> I[返回PaginationVO<br/>或List]
          I --> End([结束])
      end

      style Start fill:#d3f9d8,stroke:#2f9e44
      style End fill:#d3f9d8,stroke:#2f9e44
      style D fill:#e5dbff,stroke:#5f3dc4
      style E fill:#e5dbff,stroke:#5f3dc4
      style H fill:#ffe8cc,stroke:#d9480f

  查询条件包括：
  - lessonId - 课次ID
  - studentId - 学员ID
  - teacherId - 老师ID
  - keyword - 关键词
  - startDate/endDate - 日期范围
  - onlyEvaluate - 仅查询有点评的记录

  ---
  2. 设置学员上课状态（支持多选-点名）

  graph TB
      subgraph 点名流程
          Start([开始]) --> A[POST /common/lesson/rollCallBatch]
          A --> B[接收List&lt;LessonSignSaveDTO&gt;<br/>支持多选]
          B --> C[遍历校验每个DTO]
          C --> D{状态枚举<br/>是否合法?}
          D -->|否| E[返回错误:<br/>点名状态有误]
          D -->|是| F[获取当前教师ID]
          F --> G[调用lessonService<br/>.rollCallBatch]
          G --> H[遍历处理每个签到记录]
          H --> I{学员记录<br/>是否已存在?}
          I -->|已存在| J[调用addRecord<br/>更新签到状态]
          I -->|不存在| K[创建新的<br/>LessonStudent记录]
          J --> L{签到状态<br/>是否需要消课?}
          K --> L
          L -->|是| M[调用decLessonCount<br/>扣减学员课时]
          L -->|否| N[不扣课时]
          M --> O[更新/新增<br/>签到记录]
          N --> O
          O --> P[返回成功]
          P --> End([结束])
          E --> End
      end

      style Start fill:#d3f9d8,stroke:#2f9e44
      style End fill:#d3f9d8,stroke:#2f9e44
      style M fill:#ffe3e3,stroke:#c92a2a
      style O fill:#ffe8cc,stroke:#d9480f

  签到状态枚举（SignStateEnum）：
  - NONE(0) - 未签到
  - NORMAL(1) - 已签到
  - LATE(2) - 补签
  - LEAVE(3) - 请假
  - ABSENT(4) - 旷课

  ---
  3. 停课/复课

  graph TB
      subgraph 停课/复课流程
          Start([开始]) --> A{操作类型}
          A -->|停课| B[POST /common/lesson/stop]
          A -->|复课| C[POST /common/lesson/reopen]

          B --> D[接收课次ID列表<br/>List&lt;Long&gt;]
          C --> D
          D --> E[调用lessonService<br/>批量查询课次]
          E --> F[遍历每个课次]
          F --> G{操作类型?}
          G -->|停课| H[设置状态=<br/>STOPPED(0)]
          G -->|复课| I[设置状态=<br/>UNDERWAY(1)]
          H --> J[调用sendLessonStateChangeNotice<br/>发送通知]
          I --> J
          J --> K[updateBatchById<br/>批量更新]
          K --> L[返回成功/失败]
          L --> End([结束])
      end

      style Start fill:#d3f9d8,stroke:#2f9e44
      style End fill:#d3f9d8,stroke:#2f9e44
      style H fill:#ffe3e3,stroke:#c92a2a
      style I fill:#d3f9d8,stroke:#2f9e44
      style K fill:#ffe8cc,stroke:#d9480f

  课次状态枚举（LessonStateEnum）：
  - STOPPED(0) - 已停课
  - UNDERWAY(1) - 进行中
  - COMPLETE(2) - 已结课

  ---
  4. 还原（消课返还还原）

  graph TB
      subgraph 还原流程
          Start([开始]) --> A[POST /common/lessonStudent<br/>/rollbackCourseNum]
          A --> B[接收签到记录ID列表<br/>List&lt;Long&gt;]
          B --> C[遍历校验每个ID]
          C --> D{记录是否存在?}
          D -->|否| E[抛出异常:<br/>无法还原无签到状态的记录]
          D -->|是| F{是否有消费课程ID?}
          F -->|否| G[抛出异常:<br/>无法还原未设置消费课程的记录]
          F -->|是| H[添加到处理列表]
          E --> End([失败])
          H --> I[遍历处理列表]
          I --> J[获取实扣课次数<br/>decLessonCount]
          J --> K[设置signState=NONE<br/>未签到状态]
          K --> L[设置decLessonCount=0<br/>清零消课数]
          L --> M{实扣次数<br/>&gt;0?}
          M -->|是| N[调用rollcallCancel<br/>返还课时]
          M -->|否| O[不返还]
          N --> P[记录课时变动日志<br/>handleAdd]
          O --> Q[updateById<br/>更新记录]
          P --> Q
          Q --> R[返回成功/失败]
          R --> End([结束])
      end

      style Start fill:#d3f9d8,stroke:#2f9e44
      style End fill:#d3f9d8,stroke:#2f9e44
      style N fill:#ffe3e3,stroke:#c92a2a
      style Q fill:#ffe8cc,stroke:#d9480f